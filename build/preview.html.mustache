<!DOCTYPE html>
<html lang="en" translate="no">
<head>
    <meta charset="UTF-8">
    <meta name="google" content="notranslate">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Path Icons Preview</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
            background-color: #f5f5f5;
        }

        .icon-container {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            align-items: center;
            margin-bottom: 16px;
        }

        .icon-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 4px;
            background-color: white;
            text-align: center;
            min-width: 60px;
        }

        .icon-name {
            font-size: 10px;
            margin-top: 4px;
            word-break: break-all;
        }

        .icon-item span[class^="pi-"] {
            --pi-size: 32px;
            --pi-scale: 2.0;
        }

        .icon-toolbar {
            border: 1px solid #ccc;
            padding: 8px;
        }

{{! Embed the generated CSS content here using triple braces for unescaped output }}
{{{cssContent}}}

    </style>
</head>
<body>
    <h1>Path Icons Preview</h1>
    <div class="icon-container">
        {{#icons}}
        <div class="icon-item">
            <span class="pi-{{name}}"></span>
            <span class="icon-name">{{header}}</span>
        </div>
        {{/icons}}
    </div>
    <div class="icon-toolbar">
        {{#icons}}
        <i class="pi-{{name}}"></i>
        {{/icons}}
    </div>
</body>
</html>